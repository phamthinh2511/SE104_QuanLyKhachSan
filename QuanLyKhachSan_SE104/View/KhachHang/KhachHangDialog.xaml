<Window x:Class="QuanLyKhachSan_SE104.View.KhachHang.KhachHangDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thông tin khách hàng" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border CornerRadius="20" Background="White" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.15"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" CornerRadius="20,20,0,0" Background="{StaticResource PrimaryBrush}">
                <Grid>
                    <TextBlock x:Name="TitleText" Text="👤 Thông tin khách hàng" 
                               Foreground="White" FontSize="20" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Button Content="✕" Click="Close_Click"
                            HorizontalAlignment="Right" Margin="0,0,20,0"
                            Background="Transparent" BorderThickness="0"
                            Foreground="White" FontSize="16" FontWeight="Bold" Cursor="Hand"/>
                </Grid>
            </Border>

            <!-- Body -->
            <ScrollViewer Grid.Row="1" Margin="30,20">
                <StackPanel>
                    <TextBlock Text="Họ và tên" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{StaticResource SecondaryBrush}"/>
                    <TextBox x:Name="txtName" Style="{StaticResource ModernTextBox}" Tag="Nhập họ và tên..."/>

                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Giới tính" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{StaticResource SecondaryBrush}"/>
                            <ComboBox x:Name="cbGender" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Nam"/>
                                <ComboBoxItem Content="Nữ"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Quốc tịch" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{StaticResource SecondaryBrush}"/>
                            <ComboBox x:Name="cbNationality" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Nội địa"/>
                                <ComboBoxItem Content="Nước ngoài"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="CCCD / Passport" FontWeight="SemiBold" Margin="0,15,0,8" Foreground="{StaticResource SecondaryBrush}"/>
                    <TextBox x:Name="txtIDCard" Style="{StaticResource ModernTextBox}" Tag="Số CMND/CCCD hoặc Hộ chiếu..."/>

                    <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Margin="0,15,0,8" Foreground="{StaticResource SecondaryBrush}"/>
                    <TextBox x:Name="txtPhone" Style="{StaticResource ModernTextBox}" Tag="Nhập số điện thoại..."/>

                    <TextBlock Text="Địa chỉ" FontWeight="SemiBold" Margin="0,15,0,8" Foreground="{StaticResource SecondaryBrush}"/>
                    <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}" Height="80" VerticalContentAlignment="Top" Padding="12,10" AcceptsReturn="True" TextWrapping="Wrap" Tag="Nhập địa chỉ..."/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" CornerRadius="0,0,20,20" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="💾 Lưu lại" Click="Save_Click" Style="{StaticResource PrimaryButton}" Width="120" Margin="0,0,15,0"/>
                    <Button Content="✕ Hủy bỏ" Click="Close_Click" Background="#64748B" Foreground="White" Width="100" Height="40">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
