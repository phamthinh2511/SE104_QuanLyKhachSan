<UserControl x:Class="QuanLyKhachSan_SE104.View.NhanVienView.NhanVienPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vmns="clr-namespace:QuanLyKhachSan_SE104.ViewModel.NhanVien">

    <UserControl.DataContext>
        <vmns:NhanVienViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Quản Lý Nhân Sự" FontSize="26" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>

            <ComboBox Grid.Column="1" Width="200" Margin="20,0" Style="{StaticResource ModernComboBox}"
                      ItemsSource="{Binding DanhSachMode}" SelectedItem="{Binding ModeDuocChon}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="{Binding}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding}" Value="{x:Static vmns:ModeNhanSu.NhanVien}">
                                            <Setter Property="Text" Value="Nhân Viên"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding}" Value="{x:Static vmns:ModeNhanSu.TaiKhoan}">
                                            <Setter Property="Text" Value="Tài Khoản"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <ToggleButton Grid.Column="2" Margin="0,0,20,0" Width="140" Height="40"
                          IsChecked="{Binding IsHienThiDaXoa}" Cursor="Hand">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Content" Value="✓ Đang làm việc"/>
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Setter Property="Foreground" Value="#16A34A"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content" Value="✗ Đã nghỉ việc"/>
                                <Setter Property="Background" Value="#FEE2E2"/>
                                <Setter Property="Foreground" Value="#EF4444"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding ModeDuocChon}" Value="{x:Static vmns:ModeNhanSu.TaiKhoan}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>

            <Button Grid.Column="3" Content="+ Thêm Mới" Command="{Binding AddCommand}" Style="{StaticResource PrimaryButton}"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1">
            <ContentControl Content="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ModeDuocChon}" Value="{x:Static vmns:ModeNhanSu.NhanVien}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <DataGrid ItemsSource="{Binding DanhSachNhanVien}" AutoGenerateColumns="False" IsReadOnly="True">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="ID" Binding="{Binding MaNhanVien}" Width="80"/>
                                                    <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="*"/>
                                                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                                                    <DataGridTextColumn Header="Số ĐT" Binding="{Binding SoDienThoai}" Width="120"/>
                                                    <DataGridTextColumn Header="CCCD" Binding="{Binding CCCD}" Width="150"/>
                                                    <DataGridTemplateColumn Header="Chức vụ" Width="120">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock VerticalAlignment="Center">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Text" Value="Lễ tân"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding ChucVu}" Value="True">
                                                                                    <Setter Property="Text" Value="Quản lý"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>

                                                    <DataGridTemplateColumn Header="Thao tác" Width="180">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                                    <Button Content="Sửa" Margin="0,0,6,0"
                                                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                            CommandParameter="{Binding}">
                                                                        <Button.Style>
                                                                            <Style TargetType="Button" BasedOn="{StaticResource EditButtonStyle}">
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding DataContext.IsHienThiDaXoa, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Button.Style>
                                                                    </Button>
                                                                    <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                            CommandParameter="{Binding}">
                                                                        <Button.Style>
                                                                            <Style TargetType="Button" BasedOn="{StaticResource DeleteButtonStyle}">
                                                                                <Setter Property="Content" Value="Xóa"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding DataContext.IsHienThiDaXoa, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                                                                        <Setter Property="Content" Value="Khôi phục"/>
                                                                                        <Setter Property="Background" Value="#10B981"/>
                                                                                        <Setter Property="Foreground" Value="White"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Button.Style>
                                                                    </Button>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding ModeDuocChon}" Value="{x:Static vmns:ModeNhanSu.TaiKhoan}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <DataGrid ItemsSource="{Binding DanhSachTaiKhoan}" AutoGenerateColumns="False" IsReadOnly="True">
                                                <DataGrid.Columns>
                                                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                                                    <DataGridTextColumn Header="Password" Binding="{Binding PasswordHash}" Width="150"/>
                                                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding NhanVien.HoTen}" Width="*"/>
                                                    <DataGridTemplateColumn Header="Thao tác" Width="150">
                                                        <DataGridTemplateColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                                    <Button Content="Sửa" Margin="0,0,6,0"
                                                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                            CommandParameter="{Binding}" Style="{StaticResource EditButtonStyle}"/>
                                                                    <Button Content="Xóa"
                                                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                            CommandParameter="{Binding}" Style="{StaticResource DeleteButtonStyle}"/>
                                                                </StackPanel>
                                                            </DataTemplate>
                                                        </DataGridTemplateColumn.CellTemplate>
                                                    </DataGridTemplateColumn>
                                                </DataGrid.Columns>
                                            </DataGrid>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>

                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>
    </Grid>
</UserControl>